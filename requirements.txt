# LogSentinel - Backend Dependencies

# Server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Socket.IO (async, mounted as ASGI app inside FastAPI)
python-socketio>=5.11.0

# File I/O & log watching
aiofiles>=23.2.1
watchdog>=4.0.0

# Network Operations
requests>=2.34.2
geoip2>=5.2.0

# Data processing & summary reports
pandas>=2.2.0
tabulate>=0.9.0          # required by pandas .to_markdown() — will crash without it